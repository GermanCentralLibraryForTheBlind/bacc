{
  "name": "bacc",
  "version": "0.3.0",
  "description": "BACC by DZB assist the EPUB3 production with focus on Accessibility requirements",
  "keywords": [
    "a11y",
    "accessibility",
    "checker",
    "dzb",
    "epub3",
    "epub",
    "validator",
    "wcag",
    "reading"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=7"
  },
  "author": {
    "name": "DZB developers",
    "organization": "DZB",
    "url": "https://dzb.de/",
    "email": "lars.voigt@dzb.de"
  },
  "scripts": {
    "run:frontend": "cd ./frontend && npm run start:app",
    "build:dist": "npm run clean && npm run build:frontend && npm run build:backend",
    "clean": "./backend/node_modules/.bin/rimraf dist && mkdir dist",
    "build:backend": "./backend/node_modules/.bin/rimraf dist/backend && mkdir dist/backend && mkdir dist/backend/report && mkdir dist/backend/report/locales &&  cp backend/*.js dist/backend &&  cp backend/report/*.* dist/backend/report && cp backend/report/locales/*.* dist/backend/report/locales && cp backend/package.json dist/backend",
    "build:frontend": "cd ./frontend && npm run build:dist",
    "run:backend": "node ./backend/webApi",
    "clear:uploads": "rimraf uploads && rimraf backend/uploads",
    "postinstall": "cd backend && npm install && cd ./../frontend && npm i",
    "deploy": "sh ~/Schreibtisch/update_bacc.sh",
    "preminor": "npm version minor"
  }
}
